Project Backlog: Квест «Блокировка» (QuestArrestModal)

📋 Список задач

- [x] 1. [Критический] Добавить observable-поля `isQuestArrestActive` и `arrestAnimFinished` в `MapStore`. Зависимости: нет. DoD: В конструкторе `MapStore` (после строки 78 `mapStore.jsx`) добавлены `isQuestArrestActive = false` и `arrestAnimFinished = false` — они автоматически становятся observable через `makeAutoObservable(this)`.
- [x] 2. [Критический] Добавить методы `startQuestArrest()` и `finishQuestArrest()` в `MapStore`. Зависимости: #1. DoD: `startQuestArrest()` устанавливает `isQuestArrestActive = true`, `arrestAnimFinished = false`. `finishQuestArrest()` устанавливает `isQuestArrestActive = false`, `arrestAnimFinished = false`. Оба обернуты в `runInAction`.
- [x] 3. [Критический] Переименовать кнопку в `Game.jsx` и изменить её логику: текст «Арестовать» → «Блокировать», при клике вызывать `mapStore.startQuestArrest()`, `carStore.toggleSirena()`, удалять машину из `questCars` через `removeQuestCarByIndex(index)`, сбрасывать `questCarForArrest = null`. Зависимости: #1, #2. DoD: Кнопка имеет текст «Блокировать», при нажатии запускается модалка, сирена включается, машина удаляется.
- [x] 4. [Критический] Создать компонент `QuestArrestModal.jsx`. Зависимости: #2. DoD: Компонент является observer-обёрткой, принимает `mapStore` и `carStore`, рендерит фон из `police_arrest_modal.png`, две машины (`CarModel`) с CSS-анимацией, финальная кнопка «Арестовать» появляется после завершения анимации. При нажатии вызывает `mapStore.finishQuestArrest()`, `carStore.toggleSirena()`, увеличивает `countHelp`.
- [x] 5. [Критический] Создать CSS-файл `quest_arrest.css` с анимациями. Зависимости: #4. DoD: `@keyframes targetCarDrive` (-250px → 60%, 3s), `@keyframes policeCarDrive` (-250px → 85%, 2.5s), `forwards`, `ease-out`. Стили для контейнера модалки (fixed, z-index выше UI), фоновое изображение, позиционирование машин.
- [x] 6. [Высокий] Интегрировать `QuestArrestModal` в `Game.jsx`: импорт компонента, conditional рендеринг при `mapStore.isQuestArrestActive`, импорт `quest_arrest.css`. Зависимости: #4, #5. DoD: При `isQuestArrestActive = true` в Game.jsx рендерится `<QuestArrestModal mapStore={activeMapStore} carStore={activeCarStore} />`, стили подключены.
- [x] 7. [Высокий] Обновить `instructions.md`: добавить новый квест 4 в секцию «Квесты» с последовательностью действий, условиями запуска и чек-листом для QA. Зависимости: все предыдущие. DoD: В instructions.md добавлен раздел «Квест 4: Блокировка (QuestArrestModal)» с полным описанием сценария.
- [x] 8. [Средний] Обновить `ARCHITECTURE.md`: добавить новые поля и методы `MapStore` (isQuestArrestActive, arrestAnimFinished, startQuestArrest, finishQuestArrest) в таблицу расширения контракта. Зависимости: #1, #2. DoD: ARCHITECTURE.md содержит актуальную таблицу с новыми observable-полями и методами MapStore.
