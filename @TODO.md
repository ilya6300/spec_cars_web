# Project Backlog: Fix Bensin (Fuel Canister) Component

## 📋 Список задач

- [ ] 1. [Критический] Исправить имя поля fuel в `Bensin.jsx`: заменить `carStore.max` на `carStore.maxFuel`. Зависимости: нет.
  - **DoD:** Компонент корректно рассчитывает процент заполнения канистры (fuel / maxFuel), без ошибок в консоли.

- [ ] 2. [Критический] Исправить отображение топлива: убрать `background: #737373bd` из CSS-селектора `.canister` в `interface.css` (он перезаписывает inline `background: linear-gradient(...)`), оставить только border, border-radius и другие декоративные свойства. Зависимости: #1.
  - **DoD:** Канистра не прозрачная (border, border-radius видны), фон канистры определяется inline-градиентом, визуальный контур сохранён.

- [ ] 3. [Высокий] Заменить цвет топлива в градиенте с `#34d399` на `#c3bf12bd` в `Bensin.jsx`. Зависимости: #1, #2.
  - **DoD:** Цвет заполненного топлива в канистре — жёлто-зелёный `#c3bf12bd`, визуально соответствует макету.

---
