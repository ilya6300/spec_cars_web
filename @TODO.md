# Project Backlog: SpeedDisplay — показ максимальной скорости видимых квестовых машин

## 📋 Список задач

- [x] 1. [Критический] Исправить логику SpeedDisplay: показывать только когда есть видимые на экране квестовые машины. Отфильтровать `questCars` по видимости (`screenX = positionX - distance` в пределах viewport), затем выбрать машину с максимальной скоростью. Зависимости: нет.
  - **DoD:** При 0 видимых машин — SpeedDisplay скрыт. При 1 видимой — показывает её скорость. При 2+ видимых — показывает скорость самой быстрой видимой машины.

- [x] 2. [Средний] Проверить сборку и отсутствие ошибок. Зависимости: #1.
  - **DoD:** `npx vite build` проходит без ошибок.

---
