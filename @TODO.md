Project Backlog

📋 Список задач

- [ ] 1. [Критический] Изменить начальный таймер спавна квестовых машин на 10 секунд. Зависимости: нет. DoD: `questCarSpawnTimer` инициализируется значением `10` в конструкторе `MapStore` (строка 77 `mapStore.jsx`). При запуске игры первая машина появляется через 10 секунд после старта.
- [ ] 2. [Критический] Изменить диапазон рандомного таймера спавна на 10–30 секунд. Зависимости: #1. DoD: В `spawnQuestCar()` (строка 326 `mapStore.jsx`) и `removeQuestCarByIndex()` (строка 350 `mapStore.jsx`) значение `questCarSpawnTimer` устанавливается как `10 + Math.random() * 20` (диапазон [10, 30] вместо текущего [5, 15]). После каждой смены машины таймер следующего спавна составляет от 10 до 30 секунд.
