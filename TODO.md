# Project Backlog: Правки по Квесту с другими автомобилями (Quest Cars)

## 📋 Список задач

- [x] 1. [Критический] Разделить таймеры видимости в `QuestCarStore.updateVisibility()` на 8 сек для `enemy=true` и 5 сек для `enemy=false`. Зависимости: нет.
  - **DoD:** `lastVisibleTime` сбрасывается корректно для обоих типов; `enemy=true` машины имеют таймаут 8 сек, `enemy=false` — 5 сек.

- [x] 2. [Критический] Исправить `checkQuestCarDistance()` в `MapStore` — проверка расстояния работает ТОЛЬКО для `enemy=true`. Зависимости: #1.
  - **DoD:** Кнопка "Арестовать" появляется только для `enemy=true` при дистанции `< 200px` (правый край квестовой машины в диапазоне `[policeRightEdge, maxDistance]`).

- [x] 3. [Критический] Реализовать динамическое появление/скрытие кнопки "Арестовать" в `Game.jsx` для `enemy=true`. Зависимости: #2.
  - **DoD:** Кнопка отображается только когда `questCarForArrest !== null` (машина в радиусе 200px). Кнопка исчезает, когда машина уходит из радиуса. Кнопка не отображается для `enemy=false`.

- [x] 4. [Высокий] Убедиться, что `enemy=true` квест НЕ сбрасывается при уходе машины за экран — машина может вернуться. Зависимости: #1.
  - **DoD:** `questCarActive` остаётся `true` пока машина не арестована; массив `questCars` не очищается при `enemy=true` уходе за экран; при возврате машины на экран кнопка "Арестовать" снова появляется при сближении.

- [x] 5. [Высокий] Исправить логику удаления `questCar` из массива в `MapStore.updateQuestCars()` — не удалять `enemy=true` из `questCars` при уходе за экран, удалять только `enemy=false` после 5 сек. Зависимости: #1, #4.
  - **DoD:** `enemy=true` машины остаются в массиве `questCars` даже при `active=false` (или помечаются как `active=false` но не удаляются); `enemy=false` машины удаляются после 5 сек невидимости.

- [x] 6. [Средний] Добавить флаг `questCarDismissed` в `QuestCarStore` для отслеживания полностью завершённых квестов (`enemy=true` арестован или `enemy=false` уехал). Зависимости: #1, #5.
  - **DoD:** При аресте (`enemy=true`) или уходе за экран (5 сек, `enemy=false`) квест помечается как завершённый; `questCarActive` сбрасывается в `false`; новая машина может спавниться.

- [x] 7. [Средний] Unit-тесты для `QuestCarStore.updateVisibility()` — проверка таймеров 8 сек / 5 сек. Зависимости: #1.
  - **DoD:** Тесты покрывают: `enemy=true` не деактивируется до 8 сек; `enemy=false` деактивируется после 5 сек; возврат на экран сбрасывает таймер.

- [ ] 8. [Средний] E2E-тесты Playwright для квеста с автомобилями: проверка появления/скрытия кнопки "Арестовать" для `enemy=true`. Зависимости: #3, #6.
  - **DoD:** Тест проверяет: машина `enemy=true` подъезжает → кнопка появляется → машина отъезжает → кнопка исчезает → арест → кнопка исчезает навсегда.
