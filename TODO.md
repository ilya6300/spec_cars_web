# Project Backlog — Отслеживание светофора и anti-overlap объектов

📋 Список задач

- [x] 1. [Критический] Исправить логику `trafficLightOnTheMap` в MapStore: установить `true` при появлении светофора в `triggerAppearEvents()`, корректно сбрасывать в `despawnObjects()`. Зависимости: нет. DoD: флаг меняется при появлении/исчезновении светофора на экране.

- [x] 2. [Критический] Добавить computed-свойство `isTrafficLightRed` в MapStore, возвращающее `true` если светофор на экране И его цвет красный. Зависимости: #1. DoD: свойство доступно для чтения, возвращает корректный boolean.

- [x] 3. [Высокий] Подключить MapStore к carStore: в `updatePhysics` заменить `console.log` на корректный вызов `checkTrafficLight`. Зависимости: #1, #2. DoD: carStore получает актуальное состояние светофора без console.log.

- [x] 4. [Высокий] Реализовать метод `checkTrafficLight(mapStore)` в carStore: проверяет `trafficLightOnTheMap` и `trafficLightColor`, устанавливает `isTrafficLightOnScreen` и `trafficLightColor`. Зависимости: #2. DoD: метод корректно обновляет состояние.

- [x] 5. [Средний] Добавить computed-свойство `shouldStopForLight` в carStore. Зависимости: #4. DoD: свойство возвращает корректный boolean.

- [x] 7. [Высокий] Реализовать глобальный таймер светофора в MapStore. Зависимости: #3. DoD: светофор меняет цвет каждые 10с.

- [x] 8. [Средний] Добавить бесшовное зацикливание объектов. Зависимости: #3. DoD: объекты корректно перезапускаются.

- [x] 9. [Средний] Добавить Z-Index иерархию слоёв. Зависимости: #4. DoD: объекты корректно перекрываются.

- [x] 10. [Средний] Добавить обработку onLongPress на бензоколонке. Зависимости: #5. DoD: долгое нажатие начинает непрерывную заправку.

- [x] 11. [Критический] Исправить `refuelCar` в MapStore: убрать `return` перед блоком кода, который блокировал выполнение заправки. Зависимости: нет. DoD: вызов `refuelCar` реально обновляет топливо.

- [x] 12. [Критический] Реализовать anti-overlap: добавить `lastObjectEndMeter` в MapStore и модифицировать `spawnObjects` — новый объект спавнится только после конца последнего созданного. Зависимости: нет. DoD: объекты (дома, светофоры, бензоколонки) больше не накладываются друг на друга.

- [x] 13. [Высокий] Убрать `console.log` из `updatePhysics` в carStore. Зависимости: нет. DoD: в production-коде нет временных логов.

- [x] 14. [Высокий] Исправить `forceStop` в carStore: раскомментировать `this.currentSpeed = 0`. Зависимости: нет. DoD: при красном светофоре машина реально останавливается.

- [x] 15. [Средний] Исправить `onPointerDown` на объектах в Maps.jsx: таймер long press сохраняется на самом элементе DOM (через `e.currentTarget`), а не на объекте данных, чтобы корректно сбрасываться при `onPointerLeave`. Зависимости: нет. DoD: long press корректно отменяется при уходе курсора.
