# Project Backlog — Отслеживание светофора в carStore

📋 Список задач

- [x] 1. [Критический] Исправить логику `trafficLightOnTheMap` в MapStore: установить `true` при появлении светофора в `triggerAppearEvents()`, корректно сбрасывать в `despawnObjects()`. Зависимости: нет. DoD: флаг меняется при появлении/исчезновении светофора на экране.

- [x] 2. [Критический] Добавить computed-свойство `isTrafficLightRed` в MapStore, возвращающее `true` если светофор на экране И его цвет красный. Зависимости: #1. DoD: свойство доступно для чтения, возвращает корректный boolean.

- [x] 3. [Высокий] Подключить MapStore к carStore: в `updatePhysics` (строка ~189) заменить `console.log(MapStore.trafficLightOnTheMap)` на корректный вызов `this.checkTrafficLight(mapStore)`. Зависимости: #1, #2. DoD: carStore получает актуальное состояние светофора без console.log.

- [x] 4. [Высокий] Реализовать метод `checkTrafficLight(mapStore)` в carStore: проверяет `trafficLightOnTheMap` и `trafficLightColor`, устанавливает внутренние observable-поля `isTrafficLightOnScreen` и `trafficLightColor` (string: 'red'|'green'|null). Зависимости: #2. DoD: метод корректно обновляет состояние, используется в updatePhysics.

- [x] 5. [Средний] Добавить computed-свойство `shouldStopForLight` в carStore: `true` если светофор на экране и красный. Зависимости: #4. DoD: свойство возвращает корректный boolean, используется для автоматической остановки.ed' && !carStore.isSirenOn → \carStore.forceStop()\ (сбросить скорость до 0, не глушить двигатель). Зависимости: #3, #5. DoD: при появлении красного светофора без сирены машина останавливается принудительно; с сиреной — может проехать.

- [x] 7. [Высокий] Реализовать глобальный таймер светофора в MapStore: setInterval с периодом 10 секунд (красный 10с → зелёный 10с → повтор). Обновлять trafficLightColor. Зависимости: #3. DoD: светофор меняет цвет каждые 10с, состояние синхронизировано.

- [x] 8. [Средний] Добавить бесшовное зацикливание объектов: при уходе за левый край экрана объект перемещается вправо за пределы видимости (reset screenX). Зависимости: #3. DoD: объекты не исчезают навсегда, корректно перезапускаются при движении.

- [x] 9. [Средний] Добавить Z-Index иерархию слоёв: Фон (z=0) → Здания (z=1) → Бензоколонки (z=2) → Дорожная разметка (z=2) → Машина (z=10) → UI (z=100). Зависимости: #4. DoD: объекты корректно перекрывают/перекрываются согласно иерархии.

- [x] 10. [Средний] Добавить обработку onLongPress на бензоколонке: долгое нажатие (500ms) → \mapStore.startRefueling()\ (непрерывная заправка пока держишь). Зависимости: #5. DoD: долгое нажатие на бензоколонку начинает непрерывную заправку, отпускание — останавливает.
